---
layout: base-layout.html
title: Ernest turns 40! 🧨
---
<section class="content">
    <h1>Ernest, you've been entitle a mission</h1>
    <p>The individual in the picture above is known to be a dangerous secret 
    agent from the UK and as been causing much disturbance lately. 
    We need to take him down, but first, you will have to find as 
    much as possible about this agent. If you get the right answers to all of the 
    questions bellow, you will gain access to some sensitive data that will 
    help you fulfill your mission. Your country is counting on you.</p>
</section>

<div class="quiz-section">
<div class="content">
<h2>Quiz</h2>
<p class="bold">Please answer all the questions!</p>
</div>
<form class="content form">
{% for q in quiz %}
<h3>{{ q.QTitle }}</h3>
<p class="bold">{{ q.Question }}</p>
<div class="radio">
{% for o in q.Options %}
{% if loop.first %}
<input type="radio" name="{{ q.ID1 }}" value="{{ o }}" class="hide" checked />
{% endif %}
<label>
<input type="radio" name="{{ q.ID1 }}" value="{{ o }}" data-detail="{{ q.Details }}" data-answer="{{ q.Answer }}" data-qtitle="{{ q.QTitle }}" required />
{{ o }}
</label> 
{% endfor %}
</div>
<button class="clue-btn" id="{{ q.ID2 }}">Show Hint</button>
<p class="clue" id="{{ q.ID1 }}">{{ q.Clue }}</p>
{% endfor %}
<div class="submit-answers">
<button type="submit" id="submit-answers">Submit Answers</button>
<button type="reset" id="reset-btn">Reset</button>
</div>
</form>
</div>
<section class="content">
<h2 id="your-result">Your result <span id="percentage"></span></h2>
<div id="result"></div>
</section>